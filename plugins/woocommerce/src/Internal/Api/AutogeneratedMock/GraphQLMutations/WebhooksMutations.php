<?php

namespace Automattic\WooCommerce\Internal\Api\AutogeneratedMock\GraphQLMutations;

use Automattic\WooCommerce\Api\Webhooks;
use Automattic\WooCommerce\Internal\Api\GraphQL\GraphQLController;
use GraphQL\Type\Definition\ResolveInfo;
use GraphQL\Type\Definition\Type;

class WebhooksMutations {
	public static function get_mutations(): array {
		$container = wc_get_container();

		return array(
			'CreateWebhook' => array(
				'type'        => Type::nonNull( GraphQLController::resolve_type( 'Webhook' ) ),
				'description' => 'Create new webhook.',
				'args'        => array(
					'input' => array(
						'type' => Type::nonNull( GraphQLController::resolve_type( 'CreateWebhookInput' ) ),
					),
				),
				'resolve'     => fn( $objectValue, array $args, $context, ResolveInfo $info) => $container->get( Webhooks::class )->create_webhook( $args['input'] ),
			),
		);
	}
}
